# Приклад звіту — лабораторна робота № 2

> Зразок для **повної оцінки**: 2 програмні PNG (UART + NRZI), розрахунок часу, теорія NRZI vs 8b/10b.

---

**Титульний аркуш**

- Дисципліна: PPID  
- Лабораторна робота № 2  
- Студент: **PETRENKO**  
- Варіант: **1** (9600 8N1)

---

## 1. Мета роботи

Побудувати амплітудно-часові діаграми UART і NRZI для заданого повідомлення; розрахувати загальний час передачі UART.

## 2. Короткі теоретичні відомості

**UART:** старт-біт, 8 біт даних, 1 стоп-біт (8N1); між символами — пауза 1 такт.  
**T_такт = 1 / baudrate.**

**NRZI (USB 2.0 Full/Low Speed):** логічний 0 — зміна рівня; логічна 1 — рівень не змінюється. Після 6 послідовних одиниць вставляється bit stuffing (біт 0).

**USB 3.x SuperSpeed** використовує **8b/10b**, а не NRZI — інше кодування для вищих швидкостей і кращої синхронізації.

## 3. Хід роботи

### 3.1. Параметри варіанту

| Параметр | Значення |
|----------|----------|
| Повідомлення | `PETRENKO` (8 символів) |
| Baudrate | 9600 |
| Формат | 8N1 |
| Пауза між символами | 1 такт |

### 3.2. Діаграма UART (CLI)

```bash
python3 -m encoding.uart_plot --message "PETRENKO" --baud 9600
```

Програма вивела:

```text
Розрахований час передачі: 0.009167 с
```

**[СКРІНШОТ: вікно matplotlib з амплітудно-часовою діаграмою UART для всього повідомлення PETRENKO — збережено як `uart_petrenko.png`]**

Альтернатива — `signal_gui`, вкладка **UART**, кнопка **Save PNG**.

### 3.3. Діаграма NRZI (CLI)

```bash
python3 -m encoding.usb_nrzi --message "PETRENKO"
```

**[СКРІНШОТ: діаграма NRZI для всього повідомлення PETRENKO (bit stuffing застосовано) — `nrzi_petrenko.png`]**

Альтернатива — `signal_gui`, вкладка **NRZI**, **Save PNG**.

### 3.4. Розрахунок часу UART

```text
T_такт = 1 / 9600 ≈ 0.00010417 с
Бітів на символ (8N1) = 1 + 8 + 1 = 10
Пауза між символами = 1 такт
T_символ ≈ 11 × T_такт
T_повідомлення ≈ 8 × 11 × T_такт ≈ 0.00917 с
```

Збігається з виводом програми (0.009167 с).

### 3.5. Перевірка NRZI (pytest)

```bash
python3 -m pytest tests/test_usb_nrzi.py -v
```

```text
tests/test_usb_nrzi.py::test_nrzi_encode_basic PASSED
...
```

## 4. Висновки

Побудовано діаграми UART та NRZI для повідомлення варіанту. Час передачі UART на 9600 бод для 8 символів — близько 9.2 мс. NRZI застосовується в USB 2.0; для USB 3.x — 8b/10b.

## 5. Додаток — текст програм

Лістинги `encoding/uart_plot.py`, `encoding/usb_nrzi.py` — з розділу «Програми для виконання» методички.

## 6. Демонстрація

На захисті: показати 2 PNG + пояснити bit stuffing на прикладі одного символу в `signal_gui`.
